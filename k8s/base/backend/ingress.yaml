apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sendr-backend
  namespace: sendr
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  rules:
    - host: api.sendr.email
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sendr-backend
                port:
                  number: 8000