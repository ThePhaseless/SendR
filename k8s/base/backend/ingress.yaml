apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sendr-backend
  namespace: sendr
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
    traefik.ingress.kubernetes.io/router.tls.domains.0.main: api.sendr.email
spec:
  ingressClassName: traefik
  rules:
    - host: api.sendr.email
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sendr-backend
                port:
                  number: 8000