apiVersion: apps/v1
kind: Deployment
metadata:
  name: sendr-frontend
  namespace: sendr
  labels:
    app: sendr-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sendr-frontend
  template:
    metadata:
      labels:
        app: sendr-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: frontend
          image: ghcr.io/thephaseless/sendr/frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: API_URL
              value: "https://api.sendr.app"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi