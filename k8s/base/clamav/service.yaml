apiVersion: v1
kind: Service
metadata:
  name: sendr-clamav
  namespace: sendr
spec:
  selector:
    app: sendr-clamav
  ports:
    - name: clamd
      protocol: TCP
      port: 3310
      targetPort: clamd