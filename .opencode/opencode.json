{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "puppeteer": {
      "type": "local",
      "command": ["sh", "-c", "export DOCKER_CONTAINER=true && npx -y puppeteer-mcp-server"],
      "enabled": true
    }
  }
}
